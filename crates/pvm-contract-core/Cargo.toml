[package]
name = "pvm-contract-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core structures for PVM smart contracts sdk"

[dependencies]
ruint = { version = "1.17", default-features = false }
pallet-revive-uapi = { version = "0.10", default-features = false }
pvm-contract-types = { path = "../pvm-contract-types", default-features = false }


[features]
default = []
