error: a folded method's error type may reference `Self` only as exactly `Self::Error` (resolved via the `<Error = ...>` binding); a different associated type or a nested `Self` (e.g. `Wrapper<Self::Error>`) can't be resolved for the ABI. Write the error type concretely.
  --> tests/ui/implements_self_other_assoc_error.rs:31:40
   |
31 |         fn maybe(&self) -> Result<u64, Self::Other> {
   |                                        ^^^^^^^^^^^
